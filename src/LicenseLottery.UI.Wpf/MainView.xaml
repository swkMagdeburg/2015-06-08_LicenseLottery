<Window x:Class="LicenseLottery.UI.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="License Lottery" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
            <RowDefinition Height="5" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>

        <Label Content="Lottery Name:" Grid.Column="1" Grid.Row="1" />
        <TextBox Text="{Binding NewLotteryName, UpdateSourceTrigger=PropertyChanged}" Grid.Column="3" Grid.Row="1"
                 VerticalContentAlignment="Center" />

        <Button Command="{Binding CreateNewLottery}" Grid.Column="3" Grid.Row="3" Content="Create" />

        <Label Content="Lotteries" Grid.Column="1" Grid.Row="5" />
        <DataGrid ItemsSource="{Binding Lotteries, Mode=OneWay}" SelectedItem="{Binding SelectedLottery}"
                  AutoGenerateColumns="False" Grid.Column="3" Grid.Row="5">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name, Mode=OneWay}" Header="Name" Width="3*" />
                <DataGridTextColumn Binding="{Binding Date, Mode=OneWay}" Header="Date" Width="2*" />
                <DataGridCheckBoxColumn Binding="{Binding Finished, Mode=OneWay}" Header="Finished" Width="*" />
                <DataGridTextColumn Binding="{Binding Winner, Mode=OneWay}" Header="Winner" Width="3*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>