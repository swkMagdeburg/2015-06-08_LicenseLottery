<Window x:Class="LicenseLottery.UI.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="License Lottery" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>

        <Label Content="Lottery Name:" Grid.Column="1" Grid.Row="1" />
        <TextBox Text="{Binding NewLotteryName, UpdateSourceTrigger=PropertyChanged}"  Grid.Column="3" Grid.Row="1" VerticalContentAlignment="Center"/>

        <Button Command="{Binding CreateNewLottery}" Grid.Column="3" Grid.Row="3" Content="Create" />

        <Label Content="Created Lottery:" Grid.Column="1" Grid.Row="5" />
        <Grid Grid.Column="3" Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="5" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="5" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="5" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="5" />
            </Grid.RowDefinitions>
            
            <Label Content="Id:" Grid.Column="1" Grid.Row="1" />
            <Label Content="{Binding CreatedLottery.Id}" Grid.Column="3" Grid.Row="1" />

            <Label Content="Name:" Grid.Column="1" Grid.Row="3" />
            <Label Content="{Binding CreatedLottery.Name}" Grid.Column="3" Grid.Row="3" />

            <Label Content="Date:" Grid.Column="1" Grid.Row="5" />
            <Label Content="{Binding CreatedLottery.Date}" Grid.Column="3" Grid.Row="5" />
        </Grid>
    </Grid>
</Window>